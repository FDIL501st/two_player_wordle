# make the Web STOMP plugin use binary frames (meaning we send binary data, not UTF-8 text)
web_stomp.ws_frame = binary

# collect metrics for management plugin
deprecated_features.permit.management_metrics_collection = true